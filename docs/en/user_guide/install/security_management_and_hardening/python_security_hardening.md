# Hardening Python Security

The project involves the installation of the Python .whl packages. To prevent code tampering and unauthorized changes, you need to restrict the Python access permission when installing the Python tool and pip3 Python package manager. Specifically, limit them to be accessible and modifiable only by the installation user.
