# Anti-DoS Attack

You can limit the rate of connections to the server based on IP addresses to defense against DoS attacks, for example, by enabling the Linux iptables firewall or optimizing the `sysctl` parameter.
