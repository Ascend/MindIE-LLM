# Enabling Trustlist Verification for Host-to-Device File Transfer

On the device, trustlist verification is enabled by default during Host to Device (H2D) file transfer to verify the size, storage directory, and permission of the target file. Only files on the trustlist can be transferred from the host to the device through the HDC API.
